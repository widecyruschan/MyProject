<?phpinclude_once "config.php";include_once "config_db.php";include_once "MailClass.php";$smtpserver = $email_url; //您的smtp服务器的地址$port = $email_port; //smtp服务器的端口，一般是 25 $smtpuser = $email_username; //您登录smtp服务器的用户名$smtppwd = $email_password; //您登录smtp服务器的密码$mailtype = "HTML"; //邮件的类型，可选值是 TXT 或 HTML ,TXT 表示是纯文本的邮件,HTML 表示是 html格式的邮件$smtp  =   new smtp($smtpserver,$port,true,$smtpuser,$smtppwd,$from); $smtp->debug = false; //是否开启调试,只在测试程序时使用，正式使用时请将此行注释?>